#include <stdio.h>
#include <math.h>

 
int main() {
 
    double raio=0, esfera=(4/3.0), pi=0, VOLUME;	
	scanf("%lf", &raio);	

	pi = 3.14159;	
	
	VOLUME = (esfera*pi)*pow(raio, 3);
	printf("VOLUME = %.3lf\n", VOLUME);
	
	
    return 0;
}
